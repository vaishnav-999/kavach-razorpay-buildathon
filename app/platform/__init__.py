"""The platform plane: guard, mandate, payments, webhooks, audit.

`razorpay_client` lives here and may be imported only from inside this package
(§3.1, invariant 1). The buyer agent has no path to Razorpay at all.
"""
